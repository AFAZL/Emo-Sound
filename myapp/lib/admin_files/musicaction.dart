import 'package:flutter/material.dart';

class MusicAction extends StatelessWidget {
  const MusicAction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}